1. download the code from given repository
2. navigate to root folder from command prompt
3. install playwright using following command
   npm install playwright
4. install browsers
   npx playwright install
4. run the tests using following command
   npx playwright tests

report will get open automatically after tests got completed with configuration which mentioned in playwright.config.js